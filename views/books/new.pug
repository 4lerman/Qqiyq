extends ../layouts/layout.pug

block content
    h2(class='page-header') New Book
    form(action='/books' method='POST')
        include _form_fields
        a(href='/books') Cancel
        button(type='submit') Create