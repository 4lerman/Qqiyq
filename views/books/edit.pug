extends ../layouts/layout.pug

block content
    h2(class='page-header') Edit Book
    form(action=`/books/${book.id}?_method=PUT` method='POST')
        include _form_fields
        a(href='/books') Cancel
        button(type='submit') Create