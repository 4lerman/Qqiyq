extends ../layouts/layout.pug

block content
    h2(class='page-header') Edit Author
    form(action=`/authors/${author.id}?_method=PUT` method='POST')
        include _form_fields
        a(href=`/authors/${author.id}`) Cancel
        button(type='submit') Update