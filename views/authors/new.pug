extends ../layouts/layout.pug

block content
    h2(class='page-header') New Author
    form(action='/authors' method='POST')
        include _form_fields
        a(href='/authors') Cancel
        button(type='submit') Create